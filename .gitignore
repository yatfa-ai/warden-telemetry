node_modules/
# The durable store is runtime data, not source — never commit a receiver's
# persisted telemetry.
*.ndjson

# Rendered k8s secrets (contain AUTH_TOKEN). Templates (.template) ARE committed;
# the rendered secret.yaml is generated locally by render-secret.sh and never
# committed.
secret.yaml
deployment/manifests/secret.yaml
