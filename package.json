{
  "name": "warden-telemetry",
  "version": "0.1.3",
  "description": "Self-hostable telemetry receiver for Yatfa Warden — accepts, schema-validates, and durably persists optional, off-by-default telemetry events. The receiver half of Warden's telemetry system.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --experimental-strip-types --test"
  },
  "license": "MIT",
  "devDependencies": {
    "specguard-ts": "github:yatfa-ai/specguard-ts"
  }
}
